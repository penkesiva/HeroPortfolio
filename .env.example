# Supabase (Auth: Google OAuth, email/password, magic link, password reset).
# Copy to .env.local and fill from:
# https://supabase.com/dashboard/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=https://lthmvormuwtbtaunyske.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_4s85B_izfVIoBerphGWL_g_D5AuV_ZE
#
# Dashboard → Authentication → Providers: enable Email and Google (add OAuth client ID/secret).
#
# Dashboard → Authentication → URL configuration → Redirect URLs (add each).
# Include query variants Supabase uses for ?next= (OAuth, magic link, password reset):
#   http://localhost:3000/auth/callback
#   http://localhost:3000/auth/callback?next=%2Ftimeline
#   http://localhost:3000/auth/callback?next=%2Fauth%2Freset-password
#   https://YOUR_DOMAIN/auth/callback
#   https://YOUR_DOMAIN/auth/callback?next=%2Ftimeline
#   https://YOUR_DOMAIN/auth/callback?next=%2Fauth%2Freset-password

# Optional: show content editor without ?edit=1 in production
# NEXT_PUBLIC_CONTENT_EDITOR=true
